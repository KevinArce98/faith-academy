
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Native prebuild output (managed workflow vía app.config.js — se regenera con
# `expo prebuild` / `expo run:ios` / `expo run:android`, nunca se versiona).
/ios
/android

# Expo build cache (no versionar)
.expo/